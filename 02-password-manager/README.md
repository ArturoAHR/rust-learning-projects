# Password Manager

## Commands

- `init` To set up your master password and password storage.
- `add` Adds a password under the given name.
- `get` Allows you to get a password identified by the given name.
- `list` Displays all the registered names.

If you haven't registered your master password, you'll be prompted to initialize the password manager.